import React from 'react';

const Searchbar = () => {
  return (
    <div>
      <input
        type="search"
        name="search"
        className="search"
        placeholder="Search..."
      />
    </div>
  );
};

export default Searchbar;
